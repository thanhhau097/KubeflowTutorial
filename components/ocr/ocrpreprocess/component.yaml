name: OCR Preprocess
description: Performs OCR preprocess
inputs:
  - {name: Data Path, description: 'Input Path'}
outputs:
  - {name: Output Path, description: 'Output Path'}
implementation:
  container:
    image: index.docker.io/thanhhau097/loaddata
    command: [
      python3, /pipelines/component/src/main.py,
      --data_path,  {inputValue: Data Path},
      --output_path, {outputPath: Output Path},
    ]
